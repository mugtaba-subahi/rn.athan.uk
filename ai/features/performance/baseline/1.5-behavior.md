# Baseline 1.5 — Behavior snapshot (the 1:1 contract)

## Jest

- **918/918 passed, 35 suites** (progress.md said 876 — suite grew since; new count is the
  reference). `yarn test` 20.6s.

## Live assertion flows verified (Release builds, physical devices)

- Android (Maestro CLI): settings sheet open/close ×20 green; pager Standard↔Extras with
  page-label assertions ×15 pairs green; sound sheet open + row selects + double-back close
  green; overlay open/close ×20 end-state green.
- iOS (WDA): home content ('London, UK', prayer rows, countdown labels) verified; settings
  sheet open (chrome) + swipe close ×20; overlay open/close ×20; pager swipes ×30; toggle
  + sound paced suites; notif permission granted on first launch.

## AX captures

- `ios-home-ax.xml` (JSON-wrapped XCUI tree, iPhone XS home screen).
- Android home + settings-sheet trees captured via Maestro inspect during session 2
  (labels/coordinates recorded in 1.2 harness notes; uiautomator dump broken on this 3T).

## Behavior quirks to preserve / investigate (NOT to change silently)

1. Backdrop tap does NOT dismiss sheets (pressBehavior none) — by design.
2. Android hardware back closes sheet stack; **back from sound sheet closed BOTH sheets**
   (possible BackHandler double-registration — flag to owner; not a perf change).
3. Android AX keeps covered elements "visible" (assert via overlays' duplicate labels).
4. iOS sheet content is AX-invisible in Release (chrome only) — harness works around it.
5. Pager swipes only page from the y≈21% band (rows consume gestures).
